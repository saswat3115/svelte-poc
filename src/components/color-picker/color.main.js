import ColorPicker from "./color.svelte";

// const app = new App({
//   target: document.body
// });

// export default app;
// console.log("main", CUSTOM_ELE);
// const colorPicker = CUSTOM_ELE
//   ? new ColorPicker()
//   : new ColorPicker({ target: document.getElementById("dash") });

export default new ColorPicker();
